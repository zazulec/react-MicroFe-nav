import './navBar.css'
const NavBar = () => {
    return (
        <nav className="topnav">
            <a className="active" href="#home">Home</a>
            <a href="#blog">Blog</a>
        </nav>
    );
};

export default NavBar;
