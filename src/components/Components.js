import React from 'react';

const Components = () => {
    return (
        <div>
            testowy Komponent (Nav repo src/components/Components.js)
        </div>
    );
};

export default Components;
